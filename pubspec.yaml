name: Proiecte_Android_Flutter
description: A new Flutter project. This is the demo project for Google Programming Hub Flutter Course 1.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev


version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter


  cupertino_icons: ^1.0.0
  http : ^0.12.2

dev_dependencies:
  flutter_test:
    sdk: flutter

# The following section is specific to Flutter.
flutter:


  uses-material-design: true

